'use client';
import {useEffect,useMemo,useState} from 'react';
export default function Codeforces(){const [data,setData]=useState<any[]>([]);const [min,setMin]=useState('0');const [max,setMax]=useState('4000');const [tag,setTag]=useState('');const [q,setQ]=useState('');useEffect(()=>{fetch('/api/codeforces').then(r=>r.json()).then(x=>setData(x.problems||[]))},[]);const tags=Array.from(new Set(data.flatMap(x=>x.tags||[]))).sort();const filtered=useMemo(()=>data.filter(x=>(x.rating||0)>=Number(min)&&(x.rating||99999)<=Number(max)&&(!tag||x.tags?.includes(tag))&&(!q||x.name.toLowerCase().includes(q.toLowerCase()))).slice(0,1000),[data,min,max,tag,q]);return <div className="page"><div className="eyebrow">CODEFORCES · LIVE PROBLEMSET</div><h1>Rating-wise Practice</h1><p className="muted">Choose a rating range and topic. Problems open directly on Codeforces.</p><div className="toolbar"><input placeholder="Search problem" value={q} onChange={e=>setQ(e.target.value)}/><select value={min} onChange={e=>setMin(e.target.value)}><option value="0">Min rating</option>{[800,900,1000,1100,1200,1300,1400,1500,1600,1700,1800,2000,2200,2400].map(x=><option key={x}>{x}</option>)}</select><select value={max} onChange={e=>setMax(e.target.value)}><option value="4000">Max rating</option>{[1000,1200,1400,1600,1800,2000,2200,2400,2600,3000].map(x=><option key={x}>{x}</option>)}</select><select value={tag} onChange={e=>setTag(e.target.value)}><option value="">All tags</option>{tags.map(x=><option key={x}>{x}</option>)}</select></div><div className="list">{filtered.map(x=><a className="problem" target="_blank" rel="noreferrer" href={`https://codeforces.com/problemset/problem/${x.contestId}/${x.index}`} key={`${x.contestId}-${x.index}`}><div><b>{x.name}</b><div className="small muted">{(x.tags||[]).join(' · ')}</div></div><span className="badge">{x.rating||'Unrated'}</span><span>↗</span></a>)}</div></div>}
